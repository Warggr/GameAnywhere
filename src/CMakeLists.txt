add_subdirectory(agents)
add_subdirectory(network)

#find_package(Python COMPONENTS Interpreter Development.Embed)

#add_library(python_runtime STATIC python_runtime.c)
#message("Python: ${Python_INCLUDE_DIRS}")
#target_include_directories(python_runtime PRIVATE ${Python_INCLUDE_DIRS})
